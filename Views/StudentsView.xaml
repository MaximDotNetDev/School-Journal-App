<UserControl x:Class="SchoolJournalApp.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="700" d:DesignWidth="1000"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#E3F2FD" Offset="0"/>
            <GradientStop Color="#E0F7FA" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <TextBlock Text="Учні" FontSize="28" FontWeight="Bold" Foreground="#102A43" Margin="0,0,0,5"/>
            <TextBlock Text="Особові справи" FontSize="14" Foreground="#546E7A" Margin="0,0,0,30"/>

            <Border Background="White" CornerRadius="15" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Opacity="0.05" ShadowDepth="5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="🏫 Оберіть клас" FontWeight="SemiBold" Foreground="#0277BD" Margin="0,0,0,10"/>
                    <ComboBox x:Name="cmbClasses" Style="{StaticResource ModernComboBox}"
                              SelectionChanged="CmbClasses_SelectionChanged" 
                              DisplayMemberPath="ClassName" SelectedValuePath="ClassID"/>

                    <TextBlock Text="👤 Оберіть учня" FontWeight="SemiBold" Foreground="#0277BD" Margin="0,20,0,10"/>
                    <ListBox x:Name="listStudents" Height="350" Background="White" BorderThickness="1" BorderBrush="#E2E8F0" 
                             SelectionChanged="ListStudents_SelectionChanged" DisplayMemberPath="FullName">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Foreground" Value="#334E68"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="bd" Background="Transparent" Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="2,1">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="#E3F2FD"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="#2196F3"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                        <Button Content="➕ Додати Учня" 
            Style="{StaticResource ModernPrimaryButton}"
            Click="BtnOpenStudentForm_Click" 
            Width="125" Margin="0,0,8,0"/>
                        <Button x:Name="btnEditStudent" 
            Content="✏️ Редагувати" 
            Style="{StaticResource ModernPrimaryButton}"
            Click="BtnOpenStudentForm_Click" 
            Width="105"  IsEnabled="False"
            Visibility="{Binding IsEnabled, RelativeSource={RelativeSource Self}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Border x:Name="ProfileCard" Background="White" CornerRadius="20" Padding="30" Visibility="Collapsed" Margin="5">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="25" Opacity="0.1" Color="#90A4AE" ShadowDepth="5"/>
                </Border.Effect>

                <StackPanel>
                    <Grid Margin="0,0,0,30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Width="80" Height="80" CornerRadius="40" Background="#F3E5F5" Margin="0,0,20,0" BorderBrush="#AB47BC" BorderThickness="2">
                            <TextBlock Text="🎓" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <StackPanel VerticalAlignment="Center" Grid.Column="1">
                            <TextBlock x:Name="txtFullName" Text="Прізвище Ім'я" FontSize="24" FontWeight="Bold" Foreground="#102A43" TextWrapping="Wrap"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <Border Background="#4CAF50" CornerRadius="10" Padding="10,2" Margin="0,0,10,0">
                                    <TextBlock x:Name="txtClassBadge" Text="-" FontSize="12" Foreground="White" FontWeight="Bold"/>
                                </Border>
                                <TextBlock x:Name="txtHomeroomTeacher" Text="Класний керівник: -" VerticalAlignment="Center" Foreground="#78909C" FontStyle="Italic"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="2" VerticalAlignment="Top">
                            <Button Content="✉ Написати" Background="#2196F3" Foreground="White" Padding="15,8" BorderThickness="0" FontWeight="Bold" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="6"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <Separator Background="#E1F5FE" Margin="0,0,0,20"/>

                    <TextBlock Text="📋 Персональні дані" FontSize="18" FontWeight="Bold" Foreground="#102A43" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="День народження" Foreground="#78909C" FontSize="12"/>
                            <Border Background="#F1F8E9" Padding="10" CornerRadius="6" Margin="0,5,0,15">
                                <TextBlock x:Name="txtBirthDate" Text="-" FontWeight="Medium" Foreground="#33691E"/>
                            </Border>

                            <TextBlock Text="Стать" Foreground="#78909C" FontSize="12"/>
                            <Border Background="#F3E5F5" Padding="10" CornerRadius="6" Margin="0,5,0,15">
                                <TextBlock x:Name="txtGender" Text="-" FontWeight="Medium" Foreground="#8E24AA"/>
                            </Border>

                            <TextBlock Text="Навчальний заклад" Foreground="#78909C" FontSize="12"/>
                            <Border Background="#F1F8E9" Padding="10" CornerRadius="6" Margin="0,5,0,15">
                                <TextBlock x:Name="txtSchool" Text="-" FontWeight="Medium" Foreground="#33691E" TextWrapping="Wrap"/>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Документ" Foreground="#78909C" FontSize="12"/>
                            <Border Background="#FFF3E0" Padding="10" CornerRadius="6" Margin="0,5,0,15">
                                <TextBlock x:Name="txtDocInfo" Text="-" FontWeight="Medium" Foreground="#E65100" TextWrapping="Wrap"/>
                            </Border>

                            <TextBlock Text="Дата та підстава зарахування" Foreground="#78909C" FontSize="12"/>
                            <Border Background="#E3F2FD" Padding="10" CornerRadius="6" Margin="0,5,0,15">
                                <StackPanel>
                                    <TextBlock x:Name="txtEnrollDate" Text="-" FontWeight="Bold" Foreground="#1565C0"/>
                                    <TextBlock x:Name="txtEnrollReason" Text="-" FontSize="12" Foreground="#1565C0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Separator Background="#E1F5FE" Margin="0,10,0,20"/>

                    <TextBlock Text="👨‍👩‍👦 Інформація про батьків" FontSize="18" FontWeight="Bold" Foreground="#102A43" Margin="0,0,0,15"/>

                    <Border Background="#FAFAFA" BorderBrush="#EEEEEE" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="📞 Контакт з особової справи" Foreground="Gray" FontSize="12"/>
                            <TextBlock x:Name="txtParentContactFile" Text="-" FontWeight="Medium" Foreground="#37474F"/>
                        </StackPanel>
                    </Border>

                    <ItemsControl x:Name="listParents">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FFFDE7" BorderBrush="#FFF59D" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="📞" FontSize="20" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                            <TextBlock Text="{Binding FullName}" FontWeight="Bold" Foreground="#F57F17" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding RoleAndPhone}" Foreground="#5D4037" FontSize="13"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>